<%= render partial: "description", locals: {kitchen: @kitchen} %>

<div
  id="map"
  style="width: 85%; height: 400px; margin-top: 500 px; margin: 0 auto; border: 1px gray solid"
  data-marker="<%= @marker.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>

<%= render 'shared/footbar' %>
