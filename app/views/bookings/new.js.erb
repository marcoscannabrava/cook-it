<%# $("#bookingModal").html("<%= escape_javascript(render partial: 'booking-form', locals: { booking: @booking } ) %>");  %>
<%# $('#bookingModal').on('shown.bs.modal', function () {
  $('#bookingInput').trigger('focus')
}) %>

Rails.$('#bookingModal')[0].innerHTML = ("<%= j (render partial: 'booking-form', locals: { booking: @booking } ) %>")
